---
title: "GET /rcs/check-capability/{phone}"
sidebarTitle: "RCS Capability"
description: "GET /api/v1/rcs/check-capability/{phone} - Check whether a phone number supports RCS."
---

## Endpoint

```bash
GET https://app.sling.com.ng/api/v1/rcs/check-capability/{phone}
```

## Headers

| Header | Value |
| --- | --- |
| `Accept` | `application/json` |
| `Authorization` | `Bearer {API_TOKEN}` |

## Path parameters

<ParamField path="phone" type="string" required>
  Phone number to check. Accepts local format or country code.
</ParamField>

## Response fields

<ResponseField name="status" type="string">
  `"success"` when the lookup completes.
</ResponseField>

<ResponseField name="rcs_enabled" type="boolean">
  `true` when the phone number supports RCS.
</ResponseField>

<ResponseField name="details" type="string">
  Additional result context. Empty when RCS is enabled.
</ResponseField>

<ResponseField name="capabilities" type="array">
  Supported device capability strings.
</ResponseField>

## Capability values

| Value | Meaning |
| --- | --- |
| `chatBotCommunication` | Device can interact with RCS chatbots |
| `geolocationPush` | Device can receive and share location data |
| `callComposer` | Device supports enhanced call features |
| `chat` | Device supports standard RCS chat messaging |
| `fileTransfer` | Device can receive file attachments |

## Example responses

```json RCS enabled
{
  "status": "success",
  "rcs_enabled": true,
  "details": "",
  "capabilities": [
    "chatBotCommunication",
    "geolocationPush",
    "callComposer",
    "chat",
    "fileTransfer"
  ]
}
```

```json RCS not enabled
{
  "status": "success",
  "rcs_enabled": false,
  "details": "Number is rcs disabled or Bot is not launched with number's provider",
  "capabilities": []
}
```
